# Framework dependencies (always required)
mcp
run-mcp-servers-with-aws-lambda
boto3
httpx

# MCP server package — update to match your setup:
msgraph-mcp                                                         # from PyPI
# msgraph-mcp @ git+https://github.com/yourorg/msgraph-mcp.git     # from Git
# msgraph-mcp @ file:///absolute/path/to/local/checkout             # from local path

# Note: mcp-wrapper-runtime is installed automatically by the bundler
# from this repo — you don't need to list it here.
